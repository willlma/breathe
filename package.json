{
  "scripts": {
    "build": "web-ext build",
    "run-android": "web-ext run -t firefox-android",
    "test:debug": "playwright test --debug",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.37.1",
    "web-ext": "^8.3.0",
    "webextension-polyfill": "^0.12.0",
    "playwright": "^1.37.1"
  }
}
